@model IEnumerable<Order>

@* @{ *@
@*     ViewBag.Title = "title"; *@
@*     Layout = "_Layout"; *@
@* } *@


@* @{ *@
@*     ViewData["Title"] = "Index"; *@
@* } *@


<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">Available Orders</h2>
        </div>
        <div class="col-6 text-end">
            <a asp-controller="Home" asp-action="index" class="btn btn-primary">
                <i class="bi bi-plus-circle"></i> &nbsp; See your orders
            </a>
        </div>
    </div>
    <br/><br/>
    <div class="container">
        <table class="table table-bordered table-striped" style="width:100%">
            <thead class="text-center">
            <tr>
                <th scope="col">
                    Id
                </th>
                <th scope="col">
                    Type
                </th>
                <th scope="col">
                    Recepient
                </th>
                <th scope="col">
                    pickUp Location
                </th>
                <th scope="col">
                    dropOff location
                </th>
                <th scope="col">comments</th>

            </tr>
            </thead>
            <tbody>

            @foreach (var obj in Model) // it will display each row in database by a foreach loop that iterates over objects in Model
            {
                <tr>
                    <td>
                        @obj.Id
                    </td>
                    <td>
                        @obj.Type
                    </td>
                    <td>@obj.NameOfRecipient</td>
                    <td>@obj.PickUpLocation</td>
                    <td>@obj.DropOffLocation</td>
                    <td>@obj.Comments</td>
                    @* <td>@obj.Price</td> *@


                    <td>
                        <div class="w-75 btn-group" role="group">
                            <a asp-controller=""  asp-action="" asp-route-id=""
                               class="btn btn-success m-2">
                                <i class="bi bi-pencil-square"></i> Take Order
                            </a>
                        </div>
                    </td>
                </tr>
            }
            </tbody>

        </table>
    </div>
</div>